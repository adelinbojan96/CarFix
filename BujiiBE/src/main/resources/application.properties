# Spring Boot Application Name and Port
spring.application.name=BujiiBE
server.port=8080

# Database (Railway PostgreSQL URL)
spring.datasource.url=jdbc:postgresql://junction.proxy.rlwy.net:49325/railway
spring.datasource.username=postgres
spring.datasource.password=HHqQZcdYHLJaDyHtwAGNwTUWxuwLeRpC
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=5

# Hibernate Config (No need to specify the dialect if it's deprecated)
spring.jpa.hibernate.ddl-auto=validate
# Remove deprecated dialect property:
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Disable Hibernate?s open-in-view (recommended for production environments)
spring.jpa.open-in-view=false

# JWT Secret (Keep secure in production)
jwt.secret=kpWqNNKB1uFe1Ken2+ADC/Y8IceJJrs3qAuPKJdGBrLzw5q+DeWM3ajybNmpEElW

# Logging
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG


# CORS Settings (Already set in the `CorsConfig` class)
